# Claim 5 evaluation

Status before execution: **BLOCKED**.

Acceptance requires the fixed cumulative command to generate the full
20,000/2,000 real-architecture evidence, raw checkpoint and prediction CSVs,
paired uncertainty intervals, a shuffled-pseudolabel control, an independent
checker, and passing inherited Claims 1–4. The run must record the 24-core
estimate, HF `cpu-upgrade`, actual allocation, stage runtimes, and total
runtime.

The final result will be inserted from the terminal OpenResearch run.
